{
  "name": "aryl",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "clean": "lerna run clean --parallel"
  },
  "devDependencies": {
    "lerna": "^4.0.0",
    "ts-loader": "^9.2.5",
    "typescript": "^4.3.5"
  }
}
