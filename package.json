{
  "name": "aryl",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build --stream",
    "clean": "lerna run clean --parallel",
    "test": "jest"
  },
  "devDependencies": {
    "jest": "^27.3.1",
    "lerna": "^4.0.0",
    "typescript": "^4.3.5"
  }
}
